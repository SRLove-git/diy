# 用户间聊天功能实现文档

> 状态：已实施（2026-08-03）。服务端 + 移动端 + nginx 已按本文档实现并通过冒烟测试。本文档定义一期（文字消息版）的技术实现方案。

## 1. 需求概述

用户之间可以一对一发文字消息，包括：

- 会话列表：展示所有会话（对方昵称/头像、最后一条消息预览、时间、未读数）
- 聊天页：实时收发文字消息、历史消息分页加载、发送状态（发送中/成功/失败重发）、已读回执
- 实时性：WebSocket 在线时实时推送；离线消息落库，下次上线/拉取历史可见
- 兜底：WebSocket 不可用时走 REST 发送，不阻塞发消息

一期不做：图片/语音消息（项目尚无文件上传能力）、系统级推送（APNs/FCM）、群聊、拉黑。

## 2. 技术选型

### 2.1 实时通道：WebSocket（原生 ws）

- 服务端：NestJS `@nestjs/websockets` + `@nestjs/platform-ws`（`ws` 库，与现有 HTTP 共用 3000 端口，`path: '/ws'`）
- 移动端：Dart 官方 `web_socket_channel`（稳定、无第三方兼容性风险）

选原生 ws 而非 socket.io 的原因：

| 对比项 | 原生 ws | socket.io |
|---|---|---|
| 服务端支持 | NestJS 原生 adapter | NestJS 原生支持 |
| Flutter 客户端 | 官方 `web_socket_channel`，稳定 | 社区 `socket_io_client`，版本常滞后、偶发兼容问题 |
| 自动重连/心跳 | 需自己实现（约几十行） | 内置 |
| 点对点聊天所需能力 | 已够用（无需 rooms/ack 等重特性） | 富余 |

客户端自研重连 + 应用层心跳（ping/pong）+ `clientMsgId` 发送回执，完全覆盖聊天需求。

### 2.2 为什么不用 SSE / 轮询

SSE 单向、发消息仍需 POST，语义分裂；轮询延迟高、耗流量。仅作为兜底（REST）保留。

## 3. 数据模型（MySQL 新增 2 张表）

### 3.1 conversations 会话表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK AI | |
| user_a_id | int NOT NULL | 始终存较小的用户 ID |
| user_b_id | int NOT NULL | 始终存较大的用户 ID |
| last_message_id | bigint NULL | 最后一条消息 ID（冗余，列表免 JOIN） |
| last_message_preview | varchar(255) NULL | 最后一条消息预览（`text:xxx` / `[图片]`） |
| last_message_at | datetime NULL | 最后一条消息时间，会话列表排序键 |
| created_at | datetime | |

- 唯一约束 `UNIQUE(user_a_id, user_b_id)`：保证任意两人只有一个会话
- 索引 `idx_last_at(last_message_at)`：会话列表按此排序

### 3.2 messages 消息表

| 字段 | 类型 | 说明 |
|---|---|---|
| id | bigint PK AI | 兼作游标分页键 |
| conversation_id | int NOT NULL | 所属会话 |
| sender_id | int NOT NULL | 发送者用户 ID |
| content_type | enum('text') | 一期仅 text，预留 image |
| content | text NOT NULL | 消息内容 |
| read_at | datetime NULL | 空=对方未读（对发送者而言）；非空=已读时间 |
| created_at | datetime | |

- 索引 `idx_conv_id(conversation_id, id)`：按游标（id 递减）分页拉历史

### 3.3 未读数与已读

- 未读数不单独存字段，按会话 `COUNT(messages WHERE sender_id = 对方 AND read_at IS NULL)` 实时计算（一期数据量小，直接 GROUP BY 查询）
- 已读：进聊天页时把"对方发给我的未读消息"批量置 `read_at = NOW()`

## 4. 服务端实现（新增 `server/src/chat/` 模块）

### 4.1 模块结构

```
server/src/chat/
  chat.module.ts          # 注册实体 + UsersModule + 导出 ChatService
  conversation.entity.ts
  message.entity.ts
  chat.dto.ts             # CreateConversationDto / SendMessageDto
  chat.service.ts         # 落库/查询/未读/已读 等全部业务逻辑
  chat.controller.ts      # REST API（挂 JwtAuthGuard）
  chat.gateway.ts         # WebSocket 网关（连接管理/收发/转发）
```

在 `app.module.ts` 的 imports 中注册 `ChatModule`。

### 4.2 REST API（全局前缀 /api，均需登录）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/conversations?page=` | 会话列表：对方昵称/头像 + 最后一条预览 + 未读数（`{items,total}` 数组式，对齐社区接口） |
| POST | `/conversations` `{peerUserId}` | 创建或复用与会话；peer 不存在/被封禁 → 400 |
| GET | `/conversations/:id/messages?cursor=&limit=` | 游标分页历史（id < cursor，limit 默认 50） |
| POST | `/conversations/:id/messages` `{content}` | REST 发消息（WebSocket 兜底），返回落库后的消息 |
| POST | `/conversations/:id/read` | 批量标记已读，返回 `{readAt}` |

访问控制：所有会话操作校验当前用户是会话参与者，否则 403。

### 4.3 WebSocket 协议（path `/ws`）

连接：`ws(s)://<host>:<port>/ws?token=<accessToken>`，服务端在 `handleConnection` 用 `JwtService + JWT_SECRET` 校验 token，失败立即 close。

帧格式（JSON 文本帧）：

客户端 → 服务端：

| type | 字段 | 说明 |
|---|---|---|
| `send` | `conversationId, clientMsgId, content` | 发消息 |
| `read` | `conversationId` | 标记已读 |
| `ping` | — | 心跳，客户端每 25s 发一次 |

服务端 → 客户端：

| type | 字段 | 说明 |
|---|---|---|
| `sent` | `clientMsgId, message` | 发送成功回执（含落库后完整消息） |
| `newMessage` | `message` | 新消息推送 |
| `read` | `conversationId, readerId, readAt` | 对端已读 |
| `pong` | — | 心跳应答 |
| `error` | `code, message` | 业务错误 |

### 4.4 关键逻辑

**发送流程（双写）**：收到 `send` → 校验会话/封禁 → 落库 message + 更新 conversation 冗余字段（同事务）→ 回 `sent` 给发送者 → 若对端在线转发 `newMessage`。离线用户下次拉历史可见。

**在线连接表**：Gateway 维护 `Map<userId, Set<WebSocket>>`（同用户多设备），广播时对该用户所有连接发送。

**已读流程**：收到 `read` 或 REST `/read` → 批量置 `read_at` → 若对端在线转发 `read` 事件。

**多实例扩展（一期不做，预留）**：单实例部署时进程内直接转发；多实例时用 Redis pub/sub（独立 ioredis 连接，`chat:events` 频道）转发跨实例事件，容器编排加 Redis 后即可切换。

### 4.5 依赖

```bash
npm i @nestjs/platform-ws ws
npm i -D @types/ws
```

## 5. 移动端实现（Flutter）

### 5.1 新增/修改文件

```
mobile/lib/core/chat_api.dart          # 新增：REST 调用（静态类，对齐 PostApi 模式）
mobile/lib/core/chat_service.dart      # 新增：WebSocket 连接管理 + 状态（ChangeNotifier 单例）
mobile/lib/pages/chat/conversation_list_page.dart   # 新增：会话列表
mobile/lib/pages/chat/chat_page.dart                # 新增：聊天页
mobile/lib/pages/profile_page.dart                  # 修改：加"消息"入口 + 未读角标
mobile/lib/pages/community/author_profile_page.dart # 修改：加"发消息"按钮入口
mobile/pubspec.yaml                   # 新增 web_socket_channel
```

依赖：`web_socket_channel: ^3.0.0`。不引入 uuid，clientMsgId 用 `时间戳-随机数` 生成。

### 5.2 ChatService 连接管理

- 单例 `ChangeNotifier`，跟随 `AuthService` 登录态：登录后 `connect()`，登出 `disconnect()`
- WS 地址由 `AppConfig.apiBaseUrl` 推导：`http(s)://host:port/api` → `ws(s)://host:port/ws`，携带 `?token=`
- 断线指数退避重连（1s→2s→4s…上限 30s）；`AppLifecycleState.resumed` 时立即重连
- 心跳：25s 一次 `ping`；60s 无任何帧判定断线触发重连

### 5.3 消息发送状态机

```
本地生成 clientMsgId → 上屏(pending 灰色) → WS send
  收到 sent 回执 → 更新为成功（替换真实 id/createdAt）
  10s 无回执或 WS 不可用 → REST POST 兜底
    成功 → 成功
    失败 → 失败态，点击重发（重新走 WS 或 REST）
```

### 5.4 页面设计

**会话列表页**：AppBar「消息」；ListView，每项 = 圆形头像（无头像显示昵称首字）+ 昵称 + 最后一条预览 + 相对时间 + 未读红点数字；下拉刷新。

**聊天页**：AppBar 对方昵称；消息区 `ListView(reverse)` 气泡（自己右蓝/对方左灰，`AppColors` 取色）；底部输入栏（TextField + 发送按钮，多行自适应）；上滑加载历史（游标分页）；自己消息右下角状态：发送中→已发送→已读；失败消息红色重发按钮；进入页面即 `markRead`。

**入口**：Profile 页「消息」列表项（显示未读总数角标）；作者主页「发消息」按钮直接进入会话。

### 5.5 新消息驱动

收到 `newMessage` → 更新会话列表缓存（置顶 + 预览 + 未读数+1）+ 若当前正停留该会话则插入消息并本地已读；`read` 事件 → 更新当前会话已读状态。

## 6. 部署变更

- [admin/nginx.conf](admin/nginx.conf)：新增 `/ws` location，加 Upgrade/Connection 头，反向代理到 server:3000
- 开发环境：移动端直连 server 3000 端口，无需改动
- 多实例（后续）：compose 扩容 server 时启用 Redis pub/sub（见 4.4）

## 7. 验收清单

- [x] 两个用户可互相发起会话、实时收发文字消息（冒烟测试步 1-4）
- [x] 离线消息在对方下次上线/打开会话时可见（消息落库 + 历史分页，步 6）
- [x] 会话列表正确排序（按最后消息时间）、预览、未读数准确（步 8）
- [x] 标记已读后对端收到 `read` 回执（步 5）
- [x] WebSocket 不可用时走 REST 兜底仍可实时收发（步 7）
- [x] 非会话参与者访问会话接口返回 404/403；peer 不存在/封禁返回 400（步 9 + 代码）
- [x] 服务端、移动端均通过编译与现有 lint

> 注：冒烟测试覆盖服务端全部核心链路（WS 收发/已读/REST 兜底/未读数/权限/非法 token），移动端 UI 行为按本档 5.x 实现并通过 `flutter analyze`。

## 8. 分期与后续

| 阶段 | 内容 |
|---|---|
| 一期（本次） | 文字消息、会话、历史分页、未读数、已读回执、WS 实时 + REST 兜底 |
| 二期 | 图片消息（需先补文件上传/OSS）、本地缓存（sqflite）、会话列表服务端分页 |
| 三期 | 系统级推送（APNs/FCM）、多实例 Redis pub/sub、消息审核 |

## 9. 消息延迟优化方案（P0–P3）

> 状态：P0/P1/P2/P3 已实施（2026-08-03）。针对"点击发送后停顿几秒"的问题，按优先级排出修复方案。
> 目标体验：点击发送 → 100ms 左右显示自己消息 → 300ms 以内对方收到。

### 9.1 根因摘要

| # | 根因 | 位置 | 影响 |
|---|---|---|---|
| 1 | WS 未连接时 `sendMessage` 仍空等 3s 超时 | [mobile/lib/core/chat_service.dart](mobile/lib/core/chat_service.dart) `sendMessage` | 每次发送必停顿 ~3s（主因） |
| 2 | REST 兜底为同步全量 DB 流程（约 4 次串行往返） | [server/src/chat/chat.service.ts](server/src/chat/chat.service.ts) `sendMessage` | 叠加延迟 100ms~1s+ |
| 3 | `sent` 回执在 DB 事务之后才发出（ack 延迟 = DB 延迟） | [server/src/chat/chat.gateway.ts](server/src/chat/chat.gateway.ts) `onSend` | "发送中"状态持续时间 = DB 耗时 |
| 4 | token 过期后重连仍用旧 token，被服务端 4001 关闭 | [mobile/lib/core/chat_service.dart](mobile/lib/core/chat_service.dart) `_scheduleReconnect` | 使 WS 长期不可用，触发根因 1 |
| 5 | Redis 异常时 `isUserOnline` 可能长时间挂起（ioredis 离线队列无超时） | [server/src/chat/chat.service.ts](server/src/chat/chat.service.ts) `isUserOnline` | 对端实时推送延迟，破坏 300ms 目标 |

### 9.2 P0：客户端未连接时不空等 3s（消除主因）✅ 已实施

- 文件：`mobile/lib/core/chat_service.dart`（`sendMessage`）
- 现状：`if (connected) { _sendBin(...); }` 后无论是否发出帧都 `await completer.future.timeout(3s)`。WS 未连接时 frame 未发出，completer 永远不会完成，必然等满 3s 才走 REST。
- 修改：未连接时跳过等待，立即走 REST 兜底并触发重连；仅连接成功且发出帧时才等待 ack（超时仍 3s）。

```dart
if (!connected) {
  // 未连接：不等回执，直接 REST 兜底，并尝试恢复连接
  _connect();
} else {
  _sendBin({ 'type': 'send', 'conversationId': conversationId,
             'clientMsgId': clientMsgId, 'contentType': contentType, 'content': content });
  try {
    confirmed = await completer.future.timeout(const Duration(seconds: 3));
  } on TimeoutException {
    confirmed = null;
  } finally {
    _pendingSends.remove(clientMsgId);
  }
}
if (confirmed != null) return confirmed;
// REST 兜底
```

### 9.3 P1：服务端"先 ack 后异步落库"，降低 ack 延迟 ✅ 已实施

- 文件：`server/src/chat/chat.gateway.ts`（`onSend`）、`server/src/chat/chat.service.ts`（`sendMessage`）
- 目标：`sent` 回执不再被 DB 事务阻塞；对端推送尽早发出。
- 修改：
  1. `ChatService` 拆分两步写：`saveMessage()` 仅 1 次 INSERT 即返回消息（拿到 id/createdAt）；`finalizeSend()` 异步补 message_status（2 条）与 conversations 冗余字段（可失败补偿）。
  2. `onSend` 流程改为：`await saveMessage()` → 立即回 `sent` 给发送方 → `void finalizeSend().catch(...)`（fire-and-forget）→ Redis 判断在线后推 `newMessage`。
- 兜底说明：`finalizeSend` 失败仅影响未读数/会话预览/已读回执，消息本体已落库不丢失；可后续加定时补偿任务。

### 9.4 P2：Redis 超时降级 + 重连 token 校验 ✅ 已实施

- 文件：
  - `server/src/redis/redis.module.ts`：ioredis 增加 `commandTimeout`（如 1500ms）与 `retryStrategy`，避免离线队列无限挂起。
  - `server/src/chat/chat.service.ts`：`isUserOnline` / 会话列表 `mget` 加超时保护，超时降级为"离线"（不阻塞主流程）。
  - `mobile/lib/core/chat_service.dart`（`_scheduleReconnect`）：`tryRefresh()` 失败时不要用旧 token 连接，等待下次重连周期，避免 4001 死循环。
- 可选：生产环境若移动端经域名 + nginx 访问，需启用 wss（`proxy_pass http://server:3000` 已配 Upgrade 头，补 SSL 证书即可）。

### 9.5 P3：补齐需求，完善体验 ✅ 已实施

| 事项 | 文件 | 说明 |
|---|---|---|
| 本地缓存 SQLite | `mobile/pubspec.yaml`（+`sqflite`/`path`）、新增 `mobile/lib/core/local_chat_store.dart`（messages_local 表：client_msg_id/server_id/conversation_id/sender_id/content/type/send_time/status） | 消息内存+本地双写，聊天页秒开、弱网可用；发送流水本地留底（sending→sent→read→failed 状态机，含失败可重发） |
| 图片存储抽象 | `server/src/uploads/uploads.provider.ts`（`UploadProvider` 接口 + `LocalUploadProvider` + OSS 预留）、`uploads.controller.ts`、`uploads.module.ts` | 按用户选择保持本地磁盘，multer 先写临时目录再由 Provider 落位；接 OSS/COS 时实现 Provider 并设 `UPLOAD_PROVIDER=oss` 即可 |
| 多实例 Redis pub/sub | `server/src/chat/chat.gateway.ts`（`chat:events` 频道、`instanceId` 去重、newMessage/read/presence 跨实例转发） | 在线连接表在进程内存，跨实例经 Redis pub/sub 转发；本实例直推 + publish，接收端按本地连接表 + source 去重 |
| 显式 DELIVERED 状态 | 跳过（已确认） | 单聊中 sent≈delivered，非必需 |

### 9.6 验证方法

1. 在 `ChatService._setState` 加日志观察连接状态（disconnected/connecting/connected）。
2. 发消息时观察气泡"发送中…"持续时间：恰好在 3s 左右转为"已发送" → 实锤根因 1。
3. P0 落地后：断开网络发送，应跳过 3s 等待立即走 REST 兜底。
4. 用 wscat 测 `ws://host:3000/ws` 握手，确认 WS 通道本身可用。

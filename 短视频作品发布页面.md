使用Flutter & Dart实现一个模仿抖音发布作品编辑页面，参考截图UI效果。
要求：
1. 整体是深色暗黑主题，背景#121212，页面是Scaffold，无AppBar，全部自定义布局。
2. 页面结构从上到下：
---顶部栏---
左上角返回箭头Icon(Icons.arrow_back)，右上角文字"预览"，文字白色。
---预览区域---
顶部中间：手机样式的视频预览卡片，模拟手机外框，里面放置一个占位图片（用NetworkImage占位）。
预览卡片下方：横向图片选择缩略图列表，水平ListView，每个缩略图是小Container，其中一个红色边框代表选中状态；末尾有一个+加号按钮用于添加素材。
---文本输入区域---
"添加标题" 灰色提示文字TextField，单行输入；
下方 "添加作品描述..." 多行TextField，可以输入多行文案；
---功能按钮行---
两个按钮并排：#话题按钮、@朋友按钮，圆角灰色背景，白色文字。右侧有一个方形图标按钮（排版布局按钮）。
---话题标签横向滚动列表---
SingleChildScrollView横向滚动，一批灰色圆角Chip话题标签，例如#少女时代 #叶子 #虚拟主播，可滑动。
---列表式功能选项组（每一行是右侧带>箭头的条目）
1. 选择地点：左侧定位图标+文字，右侧箭头；下方横向可滚动地点标签Chip
2. 添加标签：左侧图标+文字，右侧箭头
3. 添加自主声明：左侧喇叭图标+文字，右侧箭头

---底部操作栏---
页面最底部，从上小字提示文字："发布成功后将保存内容至本地"
最底部一行组件：
左侧：分享图标按钮
中间："限时日常"圆角深色按钮，带头像小图标
右侧：大的粉红色圆角按钮，文字"发作品"，醒目主按钮。

UI细节规范：
- 全部文字白色，hint提示文字浅灰色；背景整体深黑色；按钮背景深灰 #2c2c2c；主按钮粉色 #fe2c55
- 所有输入框无边框，只有hint提示，不需要下划线；
- 全部使用Stateless/Stateful标准flutter组件，不要第三方UI库，只用flutter自带组件。
- 布局使用Column、ListView，防止溢出，内容超出屏幕可以垂直滚动；
- 给出完整可直接运行main.dart完整代码，包含占位图片地址，注释关键布局部分。
- 适配手机屏幕，使用SafeArea。
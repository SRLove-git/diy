# 版本更新与数据安全 SOP

> 适用范围：Think Origin 手作工坊平台生产环境（`docker/compose.prod.yml`）
> 更新日期：2026-08-12

## 1. 核心原则

- **数据不在镜像里**：业务数据全部存放在 Docker 命名卷与宿主机备份目录中，更新镜像只换代码，不碰数据。
- **表结构由迁移管理**：生产环境关闭 `synchronize`，schema 变更通过 TypeORM 迁移（`server/src/migrations/`）增量执行，不删除已有数据。
- **升级前必备份，升级后必验证**：备份是回滚的唯一数据兜底。
- **只升级、不回滚 schema**：新版本上线后旧代码可能不兼容新表结构，因此故障时优先「新代码 + 修数据」，schema 回滚是最后手段。

## 2. 数据存储一览

| 数据 | 位置 | 说明 |
| --- | --- | --- |
| MySQL 业务数据 | 命名卷 `mysql-prod-data`（容器内 `/var/lib/mysql`） | 用户、预约、订单、社区内容等，最重要 |
| Redis 缓存/会话 | 命名卷 `redis-prod-data`（容器内 `/data`） | 可重建；丢失影响登录态/在线状态，不丢业务数据 |
| 本地上传文件 | 命名卷 `uploads-prod-data`（容器内 `/app/uploads`） | 仅 `UPLOAD_PROVIDER=local` 时使用；切 S3 后不落本机 |
| 每日备份 | 宿主机 `/data/diy-backups/` | `mysqldump` + Redis RDB 快照，默认保留 7 天，与数据卷分离 |

卷名在 compose 项目名下会带前缀（如 `docker_mysql-prod-data`），以 `docker volume ls` 实际为准。

## 3. 版本号与发布物约定

| 对象 | 当前约定 | 说明 |
| --- | --- | --- |
| App | `app/pubspec.yaml` 的 `version: 1.0.0+1` | 构建可用 `--build-name / --build-number` 覆盖；内测走 TestFlight / APK 直发，正式走应用商店 |
| server / admin 镜像 | 服务器源码构建生成 `diy-server:latest`、`diy-admin:latest`（`docker compose up -d --build`） | 更新部署流程见 §4 |
| 数据库 | TypeORM 迁移（`server/src/migrations/`） | 增量、幂等，启动时自动执行未跑过的迁移 |

> 现状提醒：App 端目前没有版本检查 / 强制更新机制，本 SOP 只覆盖服务端 Docker 升级与数据安全；App 端更新机制见文末待办。

## 4. 标准更新打包部署流程（SOP）

> 本章是生产环境「代码 → 服务器 → 验证」的完整操作规范。当前实际采用
> **服务器源码构建**方式（仓库代码打包 → 上传 → 服务器 `docker build` → 拉起），
> 与镜像构建两种方式任选其一即可，迁移、备份、验证流程通用。

### 4.1 部署方式总览

| 方式 | 适用场景 | 关键命令 |
| --- | --- | --- |
| 服务器源码构建（当前采用） | 默认方式：服务器性能足够、改动频繁 | 见 4.3~4.6 |
| 本地构建镜像 | 服务器不想装构建工具链 | `./docker/deploy-local.sh amd64` → `scp` + `docker load` |

三种方式最终都落到同一套 compose（`docker/compose.prod.yml`），迁移、备份、验证流程一致。

### 4.2 升级前检查

1. **确认变更范围**：`git status` / `git log`，明确本次新增的迁移文件
   （`server/src/migrations/`）与配置变更。
2. **确认备份服务健康、备份新鲜**：

```bash
docker compose -f docker/compose.prod.yml ps
ls -lh /data/diy-backups/          # 最近一次备份应 ≤ 48h
cat /data/diy-backups/.last-success
```

3. **确认生产配置完好**：`/opt/diy/docker/.env` 存在且 `DB_PASSWORD`、`JWT_SECRET`、
   `BACKUP_DB_PASSWORD` 均已配置（不要随意改动这些值，除非走 §8 轮换）。
4. 确认不在业务高峰期（建议凌晨或低峰时段）。

### 4.3 代码打包（必须排除 .env）

把仓库打包成部署包，**必须排除所有 `.env` 文件**——本地 `docker/.env`、`server/.env`
通常是占位/开发配置，打进包会覆盖服务器生产配置（曾因此导致 DB/JWT/备份密码与 OSS
配置全部失效、服务崩溃，事故详情见 §9）。

```bash
# 在仓库根目录执行
tar -czf /tmp/diy-update.tar.gz \
  --exclude='.git' --exclude='node_modules' --exclude='build' --exclude='dist' \
  --exclude='.dart_tool' --exclude='server/uploads' \
  --exclude='docker/backups' --exclude='docker/data' --exclude='.DS_Store' \
  --exclude='**/.env' \
  -C /Users/srlove/Documents/Code/diy .
```

**打包后必须校验包内没有 .env**：

```bash
tar -tzf /tmp/diy-update.tar.gz | grep -E '(^|/)\.env$' \
  && echo '!! 包内包含 .env，禁止上传' || echo 'OK：包内无 .env'
```

### 4.4 上传与解压

```bash
scp /tmp/diy-update.tar.gz root@<服务器IP>:/tmp/
ssh root@<服务器IP> \
  'cd /opt/diy && tar -xzf /tmp/diy-update.tar.gz -C /opt/diy && rm -f /tmp/diy-update.tar.gz'
```

**解压后校验生产配置未被覆盖**（与解压前记录的哈希一致；打包已排除 .env 则必然一致）：

```bash
ssh root@<服务器IP> 'md5sum /opt/diy/docker/.env'
```

> 服务器上的 `docker/.env` 是唯一的生产配置源，只通过服务器本地修改维护，
> 不入库、不打进部署包。

### 4.5 升级前备份

```bash
ssh root@<服务器IP> \
  'cd /opt/diy && docker compose -f docker/compose.prod.yml exec backup /backup.sh'
ls -lh /data/diy-backups/          # 确认出现新的 mysql-*.sql.gz / redis-*.rdb.gz
```

### 4.6 构建与拉起新版本

```bash
ssh root@<服务器IP> \
  'cd /opt/diy && docker compose -f docker/compose.prod.yml up -d --build'
```

- 默认 **3 个 server 副本**（`deploy.replicas: 3`，Compose v2+ 生效），无需 `--scale`；
  需要临时调整副本数时追加 `--scale server=N`。
- 新迁移由启动时的迁移服务在 MySQL 命名锁（`diy_migrations`，等待上限 300s）下自动
  串行执行，多副本并发安全：拿到锁的副本执行全部未跑迁移，其余副本等待后跳过。
- 构建走 Docker 层缓存（npm 缓存挂载 + 基础镜像缓存），通常几分钟完成；输出
  `BUILD_EXIT=0` 即构建成功。

### 4.7 上线验证（完整清单）

| 验证项 | 命令 | 期望 |
| --- | --- | --- |
| 容器状态 | `docker compose -f docker/compose.prod.yml ps` | 全部 `Up (healthy)`，server 3 副本 |
| 健康检查 | `curl -s http://127.0.0.1:3000/api/health` | `{"status":"ok","db":"up","redis":"up"}` |
| 迁移日志 | `docker logs docker-server-1 2>&1 \| grep -i migration` | 无 ERROR；首次出现「共执行 N 个迁移」 |
| 管理后台 | `curl -s -o /dev/null -w '%{http_code}' http://127.0.0.1:8080/` | `200` |
| 登录（带浏览器 UA） | 见下方 UA 说明 | `201` + `accessToken` |
| OSS 上传 | 登录后上传一张图片 | 返回 `thinkorigin.oss-ap-southeast-1...` 且公网 GET `200` |
| 数据抽查 | 表数量 / `users.adminRole` | 表数符合迁移预期；`admin` 为 `super_admin` |

**UA 说明**：生产已开启安全模块的 `BLOCK_BOT_UA=true`，`curl` / `python-requests` 等
脚本 UA 会被 403 拦截（`{"message":"请求被拒绝"}`）。`/api/health` 不受影响，但登录、
上传、管理接口等验证必须带浏览器 UA：

```bash
UA='Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 Chrome/120.0.0.0 Safari/537.36'
curl -s -H "User-Agent: $UA" -X POST http://127.0.0.1:3000/api/auth/login \
  -H 'Content-Type: application/json' \
  -d '{"account":"admin","password":"<密码>"}'
```

App 端（Dart/Flutter、okhttp）的 UA 在放行名单内，不受影响。

**收尾**：验证通过后，确认本次变更已提交推送（`git commit && git push`），并把
`更新策略.md` 等文档同步到服务器 `/opt/diy/`。

### 4.8 首次部署（新环境）

与升级不同，新环境从零初始化：

- 推荐直接依赖迁移建表：默认 `DB_MIGRATIONS_RUN=true`，启动时迁移目录中的
  InitialSchema 会完成建表（迁移在应用初始化前执行，见 `server/src/main.ts`）。
- 兜底方案：按 README 先设 `DB_SYNC=true` 自动建表一次，建完后**必须改回 `false`
  并重启**，之后一律走迁移。
- 首次部署需在服务器本地生成 `docker/.env`（`openssl rand -hex` 生成 DB/JWT/备份密码），
  并执行 4.7 全部验证。

### 4.9 全流程速查（一键命令）

```bash
# 1) 打包（仓库根目录）
tar -czf /tmp/diy-update.tar.gz \
  --exclude='.git' --exclude='node_modules' --exclude='build' --exclude='dist' \
  --exclude='.dart_tool' --exclude='server/uploads' \
  --exclude='docker/backups' --exclude='docker/data' --exclude='.DS_Store' \
  --exclude='**/.env' -C /Users/srlove/Documents/Code/diy .
tar -tzf /tmp/diy-update.tar.gz | grep -E '(^|/)\.env$' && echo BAD || echo ENV_OK

# 2) 上传解压
scp /tmp/diy-update.tar.gz root@<服务器IP>:/tmp/
ssh root@<服务器IP> \
  'tar -xzf /tmp/diy-update.tar.gz -C /opt/diy && rm -f /tmp/diy-update.tar.gz'

# 3) 备份 + 构建部署
ssh root@<服务器IP> 'cd /opt/diy && \
  docker compose -f docker/compose.prod.yml exec backup /backup.sh && \
  docker compose -f docker/compose.prod.yml up -d --build'

# 4) 验证
ssh root@<服务器IP> 'cd /opt/diy && \
  docker compose -f docker/compose.prod.yml ps && \
  curl -s http://127.0.0.1:3000/api/health'
```

## 5. 数据库迁移规范

- 生产 `synchronize` 关闭；`DB_MIGRATIONS_RUN=true` 时应用启动自动执行未跑迁移（框架的
  `migrationsRun` 已关闭，统一由迁移服务在 MySQL 命名锁下执行），已执行记录在 `migrations` 表，
  重复启动不会重复执行。
- 新增迁移必须**幂等**：建索引/加列前先判断是否存在（参考 `AddPerformanceIndexes` 的做法）；涉及数据清洗的迁移要兼容重复执行。
- 大表操作（加索引、回填数据）建议分批或低峰执行，避免长锁影响线上。
- 上线前先在测试环境跑一遍迁移，确认无报错再进生产。
- 禁止在生产手改表结构：会导致实体、迁移、实际 schema 三者脱节，后续 `migration:generate` 误判。

## 6. 回滚

### 6.1 代码回滚

旧镜像 tag 不可变且仍在服务器上，直接载入旧 tag 并切换即可：

```bash
docker load -i images-<旧tag>.tar.gz   # 或旧包仍在 /opt/diy
SERVER_IMAGE=diy-server:<旧tag> ADMIN_IMAGE=diy-admin:<旧tag> \
  docker compose -f docker/compose.prod.yml up -d --no-build
```

> 注意：如果本次升级跑了迁移，**旧代码可能不兼容新 schema**。此时不要盲目回滚代码，优先用新代码 + 数据修复；确需回滚代码的，先评估新加的字段/索引是否会导致旧代码报错。

### 6.2 数据回滚（恢复到备份点）

仅当数据被破坏、且可接受丢失备份点之后的所有变更时使用（最后手段）：

```bash
# MySQL：恢复最新备份
gunzip -c /data/diy-backups/mysql-<时间戳>.sql.gz | \
  docker compose -f docker/compose.prod.yml exec -T mysql sh -c \
  'exec mysql -uroot -p"$MYSQL_ROOT_PASSWORD" diy'

# Redis：先停 redis，替换 RDB 并移开 AOF 让启动时加载快照
# （卷名以 docker volume ls 实际为准，本仓库默认 docker_redis-prod-data）
docker compose -f docker/compose.prod.yml stop redis
docker run --rm \
  -v docker_redis-prod-data:/data \
  -v "/data/diy-backups:/backups:ro" \
  redis:7-alpine sh -c \
  'gunzip -c /backups/redis-<时间戳>.rdb.gz > /data/dump.rdb && \
   mv /data/appendonly.aof /data/appendonly.aof.old 2>/dev/null || true'
docker compose -f docker/compose.prod.yml start redis
```

## 7. 升级红线（禁止事项）

- **禁止 `docker compose down -v`**：`-v` 会删除数据卷，等于删除全部业务数据，且不可恢复。
- **禁止只改 `docker/.env` 的 `DB_PASSWORD`**：MySQL 数据卷初始化后账号密码已固定，必须用
  `./docker/rotate-db-password.sh` 轮换（容器内改密 → 同步 .env → 重建容器）。
- **禁止直接替换 `JWT_SECRET / JWT_REFRESH_SECRET`**：会让所有已登录用户立即掉线；必须走轮换
  （先填 `JWT_LEGACY_SECRETS` 再换新密钥）。
- **禁止把 `migration:revert` 当常规操作**：schema 回滚不是升级策略的一部分。
- **禁止跳过备份直接升级**：备份不新鲜（超过 48h，backup 健康检查会报警）时先处理备份问题。
- **禁止把本地 `.env` 打进部署包/覆盖服务器 `.env`**：`docker/.env`、`server/.env` 等
  一律从打包中排除（见 4.3「代码打包」），服务器生产配置只改 `/opt/diy/docker/.env`。

## 8. 密钥与数据库密码轮换

### 8.1 JWT 轮换（不影响已登录用户）

旧 access token（默认 2h）与 refresh token（30 天）在有效期内仍会由客户端携带使用，
直接换密钥会让它们全部失效。正确顺序：

```bash
# 1) 编辑 docker/.env：把旧密钥追加到 JWT_LEGACY_SECRETS（逗号分隔），
#    再更新 JWT_SECRET / JWT_REFRESH_SECRET 为新值
JWT_SECRET=<新密钥>
JWT_REFRESH_SECRET=<新刷新密钥>
JWT_LEGACY_SECRETS=<旧JWT_SECRET>,<旧JWT_REFRESH_SECRET>

# 2) 重启 server 生效
docker compose -f docker/compose.prod.yml up -d server
```

新令牌用新密钥签发，旧令牌用 `JWT_LEGACY_SECRETS` 验证；所有旧 access token 过期且用户
完成一次刷新后，可清空 `JWT_LEGACY_SECRETS` 并重启。

### 8.2 MySQL root 密码轮换

数据卷初始化后 `MYSQL_ROOT_PASSWORD` 环境变量不再生效，因此必须先在容器内改密，
再同步 `.env`，最后重建容器：

```bash
./docker/rotate-db-password.sh            # 自动生成随机密码
./docker/rotate-db-password.sh NewPass123 # 指定新密码
```

脚本会自动执行 `ALTER USER`（root@% / root@localhost）→ 备份并更新 `docker/.env` →
重建 mysql/server 容器。备份账号 `BACKUP_DB_PASSWORD` 独立，不受影响。

## 9. 常见故障排查

| 现象 | 处理 |
| --- | --- |
| 升级后 502 / 服务不可用 | `docker compose -f docker/compose.prod.yml ps` 看 server 是否健康；`logs server` 查报错 |
| 迁移失败 | `logs server` 找迁移 ERROR；修复迁移文件后重新出包（迁移需幂等），不要直接手改库 |
| 迁移锁获取失败 | 另一副本迁移超过 300s 或 MySQL 异常；等迁移结束重启副本 |
| 备份不新鲜 | 手动 `exec backup /backup.sh` 看报错，通常是 `BACKUP_DB_PASSWORD` 未配置或 MySQL 备份账号权限不足 |
| 副本启动后部分实例异常 | 检查 `副本数 × DB_POOL_SIZE ≤ max_connections`（默认 500） |
| 登录态集体失效 | 直接换密钥导致；按 8.1 轮换并核对 `docker/.env` |

## 10. 待办与建议

- [ ] App 端版本检查 + 强制更新接口（当前 App 内无版本检测/更新弹窗）
- [ ] 统一版本号规范（App / server / admin / 镜像 tag 对齐）
- [ ] 将升级 SOP 封装成脚本（备份 → 拉起新版本 → 迁移等待 → 健康检查）
- [ ] 定期做恢复演练：能真正恢复的备份才是有效备份

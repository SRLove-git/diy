一、布局结构（自上而下）


      系统状态栏     ← 设备层（时间、信号、电量）
      ─────────────
      导航栏         ← 页面定位（头像 + "聊天" + 筛选）
      ─────────────
      搜索栏         ← 前置功能（快速查找入口）
      ─────────────
      聊天列表       ← 核心内容区（可滚动）
      ─────────────
      FAB按钮        ← 高频操作悬浮入口（新建聊天）
      ─────────────
      底部导航栏     ← 全局模块切换（5个Tab）

    典型的"导航→筛选→内容→操作→全局导航"五层结构。


    二、设计方法


      1) 功能前置，减少层级
         - 搜索栏紧贴导航栏下方，一步可达
         - FAB按钮悬浮在右下角，随时新建对话
         - 用户从打开APP到进入聊天，最多2次点击

      2) 复用通用范式，降低学习成本
         - 圆形头像、底部Tab栏、搜索栏位置
         - 都是移动端IM的"默认语言"，用户无需学习
         - 微信、WhatsApp、Telegram都是同一套骨架

      3) 信息层级通过排版区分
         - 联系人名：黑色加粗（最高优先级）
         - 消息预览：灰色常规（次级）
         - 时间戳：灰色小字（辅助信息）
         - 三者字号和字重递减，一眼分清主次

      4) 状态提示用色彩编码
         - 蓝色圆点 = 未读消息（条目级）
         - 蓝色数字徽章 = 未读数（模块级）
         - 蓝色FAB = 新建操作（系统级）
         - 蓝色统一承担"需要关注"的语义


    三、交互设计


      短按聊天条目  → 进入对话详情
      长按聊天条目  → 弹出操作菜单（置顶/删除/标已读）
      点击搜索栏    → 激活输入 + 弹出键盘
      点击FAB按钮   → 新建聊天/选择联系人
      点击"全部"    → 展开筛选下拉（全部/未读/群聊）
      点击底部Tab   → 切换功能模块


    四、视觉手法


      色彩克制：白底 + 黑字 + 蓝色强调，干净不花哨
      留白充足：条目之间间距宽，呼吸感强
      圆角统一：搜索栏、头像、FAB按钮都用圆角
      层次分明：FAB有轻微阴影，悬浮于内容之上
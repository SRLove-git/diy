# 安全模块设计文档（Think Origin 手作工坊平台）

> 适用范围：`server/`（NestJS API）+ `app/`（Flutter 客户端）+ `admin/`（Vue 管理后台）+ `docker/`（部署）
> 更新日期：2026-08-13

## 1. 概述

本文档梳理平台「安全模块」能做什么，分两部分：

- **已实现**：代码中已落地的安全能力（标注文件位置，可审计、可验证）。
- **建议补齐**：当前缺失、但按业务规模值得上线的能力（标注优先级与落点），重点是反爬虫、恶意注册、内容安全与风控。

整体思路是**纵深防御**：请求入口（nginx/WAF）→ 应用层（认证、限流、校验、机审）→ 数据层（参数化查询、脱敏）→ 管理侧（权限、审计），每一层都能独立拦截一部分攻击。

---

## 2. 已实现的安全能力

### 2.1 账户与认证安全

| 能力 | 说明 | 代码位置 |
| --- | --- | --- |
| 密码安全哈希 | scrypt + 随机盐（16 字节）+ 64 字节输出，`timingSafeEqual` 恒定时间比较 | `server/src/auth/password.util.ts` |
| JWT 双令牌 | access token（默认 2h，`JWT_ACCESS_TTL` 可调）+ refresh token（30 天），access/refresh 密钥分离 | `server/src/auth/auth.service.ts` |
| JWT 密钥无缝轮换 | `JWT_LEGACY_SECRETS` 支持旧密钥仅验签，轮换密钥不影响在线用户 | `server/src/auth/jwt-secrets.ts` |
| refresh token 轮换 | 每次刷新签发新 refresh，旧 token 立即作废（Redis 单次使用） | `server/src/auth/auth.service.ts` |
| 登录失败锁定 | 同一账号 10 分钟内连续失败 5 次，锁定 10 分钟（Redis 计数 + 锁） | `server/src/auth/auth.service.ts` |
| 封禁/强制下线 | 封禁与踢线写入 Redis `kick:{userId}` 标记，access token 守卫、refresh 续期、WebSocket 三处同时拦截，重新登录自动清除 | `server/src/auth/session-keys.ts`、`server/src/auth/jwt-auth.guard.ts`、`server/src/chat/chat.gateway.ts`、`server/src/users/users.service.ts` |
| 设备维度防刷号 | 同一设备（MAC/安装 ID）最多注册 3 个账号；Redis 分布式锁串行化「查重+建号」，防并发绕过 | `server/src/auth/auth.service.ts`、`server/src/users/user.entity.ts`、`app/lib/api/device_id.dart` |
| 用户名唯一与修改限制 | 用户名/邮箱唯一索引；用户名一年内只能修改一次 | `server/src/users/users.service.ts`、`server/src/users/user.entity.ts` |

### 2.2 输入与数据安全

| 能力 | 说明 | 代码位置 |
| --- | --- | --- |
| 全局 DTO 校验 | `ValidationPipe({ whitelist: true, transform: true })`：未知字段自动剔除，类型/长度/格式统一校验，防注入与畸形请求 | `server/src/app.setup.ts`、`server/src/auth/auth.dto.ts` 等 |
| SQL 注入防护 | 全量 TypeORM 参数化查询（Repository / QueryBuilder 占位符），不使用字符串拼接 SQL | `server/src/**` |
| 统一异常出口 | 全局异常过滤器，内部错误不泄露堆栈细节，按语言返回友好提示 | `server/src/common/http-exception.filter.ts` |

### 2.3 内容安全（机审）

- **关键词机审**：发帖/发布作品前先做敏感词检测，命中即拦截（如「色情、赌博、诈骗、枪支、毒品」），违规内容无法上架。
- **词库动态可配**：管理端可运行时增删关键词，优先读 Redis → 环境变量 `CONTENT_KEYWORDS` → 内置默认；Redis 异常时降级，不阻断发布链路。
- **人工复核流**：帖子/短视频均有 `pending / approved / rejected` 状态，管理端可审核上架/驳回。

代码位置：`server/src/moderation/moderation.service.ts`、`server/src/community/community.service.ts`、`server/src/community/post.entity.ts`、`server/src/videos/video.entity.ts`。

### 2.4 上传安全

| 能力 | 说明 |
| --- | --- |
| MIME 白名单 | 图片 jpg/png/gif/webp；音频/视频限定常见格式，`application/octet-stream` 需扩展名兜底才放行 |
| 大小限制 | 图片/音频 10MB，短视频 1GB（multer `limits.fileSize`，超限返回友好中文提示） |
| 随机文件名 | UUID 重命名 + 随机目录，杜绝原文件名直接落到存储 |
| 路径穿越防护 | 业务目录白名单（`chat/avatar/post`），`folder` 参数不可越界 |
| 存储隔离 | 先写系统临时目录再移交 `LocalUploadProvider`/S3，S3 模式流式上传不占内存 |

代码位置：`server/src/uploads/uploads.controller.ts`、`server/src/uploads/uploads.provider.ts`。

### 2.5 传输与 HTTP 安全

| 能力 | 说明 | 代码位置 |
| --- | --- | --- |
| 安全响应头 | `helmet()` 默认开启 CSP、`X-Content-Type-Options`、`X-Frame-Options`、`Referrer-Policy` 等 | `server/src/app.setup.ts` |
| CORS 白名单 | 生产未配置 `CORS_ORIGINS` 时默认禁止跨域；配置后按完整来源严格放行 | `server/src/app.setup.ts` |
| 真实客户端 IP | nginx 反代下通过 `TRUST_PROXY=true` 正确取 `req.ip`（后续限流/风控依赖） | `server/src/app.setup.ts`、`docker/nginx/nginx.conf` |
| 上传静态托管 | 仅 `UPLOAD_DIR` 目录对外，不暴露任意文件 | `server/src/app.setup.ts` |

### 2.6 管理后台权限

- 全部管理端接口双重守卫：`JwtAuthGuard`（登录态）+ `AdminGuard`（`role === 'admin'` 且未封禁）。
- 管理端可按用户封禁、强制下线、删除作品、删除账号，形成处置闭环。

代码位置：`server/src/stores/admin.guard.ts`、`server/src/users/admin-users.controller.ts` 及 `server/src/**/admin-*.controller.ts`。

### 2.7 WebSocket 安全

| 能力 | 说明 |
| --- | --- |
| 握手鉴权 | `/ws?token=` 用 JWT 同步验签，失败直接关闭（4001） |
| 封禁即时踢线 | 已封禁/被强制下线用户的新连接与在线连接都会被关闭 |
| 连接保护 | 单用户最多 5 个连接；单实例最多 20000 连接（超限拒绝） |
| 背压保护 | 发送缓冲超 1MB 视为慢消费者，断开让其重连 |
| 帧大小限制 | 单帧最大 1MB，防止超大帧打满内存 |

代码位置：`server/src/chat/chat.gateway.ts`。

### 2.8 部署层加固

- nginx：`server_tokens off` 隐藏版本、`client_max_body_size 1g` 防超限、请求/响应超时兜底、透传真实 IP。
- 数据库：生产关闭 `synchronize`，TypeORM 迁移 + MySQL 命名锁保证多副本并发安全。
- 数据备份：每日 MySQL dump + Redis 快照，默认保留 7 天；提供数据库密码轮换脚本。
- 密钥分离：`DB_PASSWORD`、`JWT_SECRET`、`JWT_REFRESH_SECRET` 独立配置。

代码位置：`docker/nginx/nginx.conf`、`docker/compose.prod.yml`、`docker/backup/`、`docker/rotate-db-password.sh`。

### 2.9 安全一期落地（2026-08-13）

| 能力 | 实现说明 | 代码位置 |
| --- | --- | --- |
| API 全局限流 | `@nestjs/throttler` + 自研 Redis 存储（多副本共享配额）；全局按 IP 300 次/分钟（`THROTTLE_LIMIT` 可调），认证接口独立 10 次/分钟，注册/登录/刷新路由收紧到 5/5/10 次/分钟，超限封锁 5 分钟；响应带 `X-RateLimit-*` 头 | `server/src/common/redis-throttler.storage.ts`、`server/src/app.module.ts`、`server/src/auth/auth.controller.ts`、`server/src/uploads/uploads.controller.ts` |
| nginx 限流兜底 | `limit_req_zone`：全站 30r/s（突发 60）、认证接口 2r/s（突发 5）、`limit_conn` 并发限制 | `docker/nginx/nginx.conf` |
| 人机验证 | Turnstile / hCaptcha 可插拔（`CAPTCHA_PROVIDER` + 对应 secret）；开启后注册/登录无有效 `captchaToken` 一律 400 | `server/src/common/captcha.service.ts`、`server/src/auth/auth.dto.ts`、`server/src/auth/auth.service.ts` |
| IP 维度注册限制 | 同一 IP 24 小时内最多 5 个账号（`REGISTER_IP_MAX / REGISTER_IP_WINDOW_H`），Redis 计数，超限拒绝 | `server/src/auth/auth.service.ts`、`server/src/auth/auth.controller.ts` |
| 爬虫 UA / 指纹 | `BLOCK_BOT_UA=true` 时注册/登录/上传拒绝 python-requests、curl、Scrapy 等已知脚本 UA；未上报设备标识时用服务端指纹（`X-Device-Fingerprint` / UA+IP 摘要）兜底防批量注册 | `server/src/common/bot.service.ts`、`server/src/common/bot.guard.ts`、`server/src/common/security.util.ts` |
| 操作审计 | 管理端敏感操作（封禁/强制下线/删用户/审核作品/删作品/关键词/会员/活动/通知/曲库等）成功后写入 `audit_logs`，含操作人/动作/目标/请求摘要/IP/UA，敏感字段自动 `[REDACTED]`；`GET /api/admin/audit` 可按动作/操作人/时间筛选 | `server/src/audit/`、各 `admin-*.controller.ts`、`server/src/migrations/1790000000000-AddAuditLogs.ts` |
| 数据脱敏 | 管理端用户列表/搜索/封禁响应剔除 `passwordHash`，邮箱脱敏（`abc@example.com → ab***@example.com`）；会员订单管理端 `userEmail` 同样脱敏 | `server/src/users/users.service.ts`、`server/src/members/members.service.ts`、`server/src/common/security.util.ts` |

配置项全部在 `server/.env.example` 与 `docker/.env.example` 中给出默认值/说明。

---

## 3. 建议补齐的安全模块

> 以下为当前**未实现**、建议按业务阶段分批上线的能力，均标注优先级。

### 3.1 API 限流（防刷、反爬第一道闸）

现状：**已实现**应用层全局限流（Redis 存储、多副本共享）+ nginx `limit_req` 兜底，认证接口 5 次/分钟、全站 300 次/分钟。

建议后续：

1. **按用户维度限流（P1）**：发帖/评论/点赞/关注按登录用户 ID 限流（当前匿名按 IP，登录用户与匿名共用 IP 配额）。
2. **登录限流动态收紧（P1）**：连续失败后锁定时间指数增长，并对同一 IP 的多个账号登录失败做聚合计数。
3. **限流告警（P2）**：触发封锁时写入审计/告警，便于发现被盯上的目标接口。

### 3.2 反爬虫

现状：无任何反爬能力；API 全部 JSON，客户端为 App，但接口仍可被直接抓取。

建议：

| 措施 | 优先级 | 说明 |
| --- | --- | --- |
| nginx `limit_req` 按 IP 限速 | ✅ 已实现 | 见 2.9 |
| 统一入口限流 + 异常频率检测 | ✅ 已实现（应用层 + nginx） | 见 2.9；后续可加频率突增自动封禁 |
| User-Agent / 指纹识别 | ✅ 已实现（`BLOCK_BOT_UA` + `X-Device-Fingerprint`） | 见 2.9；App 端签名头待做 |
| 行为验证（人机识别） | ✅ 已实现（Turnstile/hCaptcha，需前端集成） | 见 2.9 |
| 动态签名 / 时间戳防重放 | P1 | App 与 API 间加 `X-Timestamp + 签名`，拒绝过期与重放请求（提高抓包伪造成本，配合限流使用） |
| robots.txt / 站点声明 | P2 | 对搜索引擎友好声明，限制抓取范围（仅治本之外的合规手段） |
| WAF / CDN 防护 | P2 | 上云 WAF（阿里云/腾讯云）或 Cloudflare：CC 防护、URL 黑名单、地域/ASN 封禁 |

### 3.3 恶意注册与账号滥用

现状：已有「同设备最多 3 账号」+ 唯一约束，但 `deviceId` 由客户端生成，**可被伪造**；注册无验证码、无邮箱验证、无 IP 维度限制。

建议：

| 措施 | 优先级 | 说明 |
| --- | --- | --- |
| 邮箱验证（OTP） | P1 | 注册后向邮箱发送一次性验证码（SMTP 通道已具备，缺 OTP 存储/发送/校验流程），验证通过才可发布/发言；未验证账号限制只读 |
| 图形/滑块验证码 | ✅ 已实现（Turnstile/hCaptcha） | 见 2.9；前端接入后即生效 |
| IP 维度注册限制 | ✅ 已实现 | 见 2.9；代理/数据中心 IP 黑名单待做 |
| 一次性邮箱黑名单 | P1 | 拦截 throwaway/tempmail 域名（如 `mailinator.com` 等）注册 |
| 设备指纹升级 | P1 | 用「安装 ID + 系统参数 + 网络特征」生成服务端可校验的指纹，替代纯客户端上报的 deviceId |
| 新号风控 | P1 | 新注册账号设置观察期：发帖/评论/私信频率配额更低，触发异常（群发、批量加关注）自动限权 |
| 注册频率熔断 | P2 | 全局注册速率超过阈值（如每分钟 50 个）时自动开启验证码/临时关闭注册，恢复后解除 |

### 3.4 登录安全增强

| 措施 | 优先级 | 说明 |
| --- | --- | --- |
| 失败后验证码 | P1 | 账号/IP 登录失败 1-2 次后，第 3 次起要求人机验证，降低撞库效率 |
| 新设备/异地登录提醒 | P1 | 登录 IP/设备与历史不同时，邮件或站内通知提醒 |
| 管理后台 2FA（TOTP） | P1 | 管理端登录强制 TOTP 二次验证，防止密码泄露直通后台 |
| 会话安全强化 | P2 | refresh token 绑定设备/指纹，复用旧指纹即失效；access token 缩短至 30 分钟级 |

### 3.5 内容安全升级

现状：文本关键词机审 + 人工审核已具备，但覆盖不完整、图片/视频无审核。

建议：

| 措施 | 优先级 | 说明 |
| --- | --- | --- |
| 机审覆盖全文本域 | P0 | 关键词检测扩展到评论、昵称、简介、聊天消息、视频标题/描述（当前主要覆盖发帖） |
| 变体绕过检测 | P1 | 同音字、拼音、谐音、字母数字替换（如「fǎ轮」）归一化后再匹配 |
| 图片/视频审核 | P1 | 接入云厂商内容安全（阿里云/腾讯云 OSS 内容审核）或 NSFW 模型，先审后上架 |
| 举报机制 | P1 | 用户举报 → 生成工单 → 管理端复核 → 下架/封禁联动（现有 `pending/approved/rejected` 可直接复用） |
| AI 辅助审核 | P2 | 大模型对疑似内容做二次判断，降低人工审核成本 |

### 3.6 风控与审计

| 措施 | 优先级 | 说明 |
| --- | --- | --- |
| 管理端操作审计 | ✅ 已实现 | 见 2.9；改角色等剩余操作可按 `@Audit` 继续埋点 |
| 账号关联分析 | P2 | 相同 IP/设备/邮箱模式批量注册识别：新增账号数超过阈值时自动告警或暂停 |
| 敏感数据脱敏 | ✅ 已实现（邮箱 + 密码哈希） | 见 2.9 |
| 日志脱敏 | P1 | 请求日志不落密码、token、验证码、完整邮箱 |

### 3.7 基础设施与合规

| 措施 | 优先级 | 说明 |
| --- | --- | --- |
| 全站 HTTPS + HSTS | P0 | 生产必须 TLS 终止于 nginx，配置 HSTS 与证书自动续期（当前 compose 只有 80 端口） |
| 密钥管理 | P2 | JWT/DB 密钥引入 KMS/Vault 或至少使用独立 secret 文件，避免明文进镜像 |
| 安全响应头按需强化 | P2 | 评估 CSP 收紧（当前 helmet 默认配置偏宽松），管理后台加 `frame-ancestors` 限制 |
| 依赖漏洞扫描 | P2 | CI 加 `npm audit` / Dependabot，定期升级有 CVE 的依赖 |

---

## 4. 优先级路线图

| 阶段 | 目标 | 包含内容 |
| --- | --- | --- |
| P0（上线必须） | 挡住自动化攻击与恶意注册 | ✅ API 全局限流、✅ 人机验证、IP 注册限制、✅ UA/指纹拦截、✅ 操作审计、✅ 数据脱敏；剩余：邮箱 OTP 验证、HTTPS+HSTS、机审覆盖全部文本域 |
| P1（上线后 1-2 个月） | 反爬与风控成型 | 动态签名防重放、一次性邮箱黑名单、设备指纹升级、新号风控、失败后验证码、异地登录提醒、管理端 2FA、图片/视频审核、举报机制、按用户限流 |
| P2（持续演进） | 纵深与合规 | WAF/CDN、AI 审核、账号关联分析、KMS、依赖扫描、CSP 收紧 |

---

## 5. 建议落点（代码位置）

| 能力 | 建议实现位置 |
| --- | --- |
| 全局限流守卫 | 新建 `server/src/common/throttler.guard.ts`，注册进 `server/src/app.setup.ts`；登录/注册在 `server/src/auth/auth.controller.ts` 细化配额 |
| nginx 限流 | `docker/nginx/nginx.conf` 增加 `limit_req_zone` 与各 `location` 的 `limit_req` |
| OTP 验证 | 复用 `server/src/email/email.module.ts` 邮件通道，新增 `server/src/auth/otp.service.ts`（Redis 存码、5 分钟过期、6 位随机、重发限频） |
| 人机验证 | 注册/登录 DTO 增加 `captchaToken` 字段，`server/src/common/captcha.service.ts` 校验 Turnstile/hCaptcha |
| IP 注册限制 | 在 `server/src/auth/auth.service.ts` 的 `register()` 内按 `req.ip` 计数 |
| 机审扩展 | 在 `server/src/moderation/moderation.service.ts` 增加归一化检测；调用点扩展到评论、昵称、聊天 |
| 审计日志 | 新建 `server/src/audit/` 模块，在 `admin-*.controller.ts` 敏感操作处埋点 |
| HTTPS | `docker/nginx/nginx.conf` 增加 443 server 与 HSTS 头，compose 挂证书目录 |

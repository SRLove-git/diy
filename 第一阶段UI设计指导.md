# 第一阶段 UI 设计指导 · DIY 手作工坊平台 App

> 版本：V1.0 ｜ 日期：2026-08-02 ｜ 适用范围：一期 iOS / Android 客户端
> 对齐文档：《DIY 手作工坊平台-乙方执行方案书 V1.0》／《schedule.md》

---

## 一、设计目标

一句话目标：**让用户「一看就懂、一用就会」，2~3 步完成预约，随手就能分享作品。**

- **功能简单**：一期只保留核心链路（预约 → 到店 → 分享 → 互动），非核心功能一律二期。
- **页面简洁**：单页聚焦一个任务，删除一切装饰性、说明性冗余。
- **文字精简**：标题不超过 6 个字，按钮 2~4 个字，辅助说明一句以内。
- **低成本**：核心操作全程 ≤3 步、≤3 次点击可达；新手无需教程。

---

## 二、UI 设计原则（全局约束）

| # | 原则 | 说明与落地要求 |
| --- | --- | --- |
| 1 | 极简优先 | 每页只承载 1 个核心任务；不添加无功能意义的装饰；能删则删 |
| 2 | 一步直达 | 核心流程 ≤3 步完成；首屏直接呈现可操作内容（如预约页直接展示附近门店） |
| 3 | 全端一致 | 一套设计系统贯穿所有页面：同一操作永远同一种表达；组件复用不造轮子 |
| 4 | 反馈即时 | 每个操作都有明确反馈：成功 / 失败 / 进行中；关键状态（核销、上/下钟、会员到期）常显可见 |
| 5 | 层级清晰 | 一眼看到最重要信息；每屏只有一个主按钮（主色实心），次操作降级为描边/文字 |
| 6 | 信任透明 | 价格、时间、桌位、时长、会员权益等数据一目了然，不藏不绕 |
| 7 | 空态友好 | 空数据时给一句引导 + 一个行动按钮，不让用户面对空白 |
| 8 | 无障碍底线 | 正文文字对比度 ≥ 4.5:1；支持系统字体缩放；点击区域 ≥ 44×44pt |

---

## 三、全局视觉规范

### 3.1 色彩

| 角色 | 色值 | 用途 |
| --- | --- | --- |
| 主色 | #E8633A（陶土橙） | 主按钮、选中态、品牌标识、会员标识 |
| 主色浅底 | #FFF0E8 | 选中背景、标签底色 |
| 页面背景 | #F7F5F2（米白） | 全局背景 |
| 卡片 | #FFFFFF | 卡片、列表 |
| 主文字 | #2B2B2B | 标题、正文 |
| 次要文字 | #8A8A8A | 辅助说明、时间戳 |
| 分割线 | #ECEAE6 | 列表分隔 |
| 成功 | #2E9E5B | 核销成功、下钟、可约 |
| 警示 | #E6A23C | 即将到期、人数预警 |
| 危险 | #D9453E | 取消、不可约 |

整体走「手作工坊」的温暖感：暖橙 + 米白 + 深灰，避免高饱和刺眼色块。

### 3.2 字体与字号

- 字体：iOS PingFang SC，Android 系统默认字体。
- 字号阶梯：24（页面大标题）／20（模块标题）／17（正文）／15（次要）／13（辅助、标签）。
- 时间、价格、时长、验证码等数字用等宽/粗体突出，保证一眼读准。

### 3.3 间距 · 圆角 · 图标

- 间距：4 的倍数（4/8/12/16/20/24）；页面左右边距 16；卡片内边距 16；卡片间距 12。
- 圆角：卡片 12、按钮 10、输入框 10、弹窗 16、头像/标签圆形。
- 图标：线性风格、24×24pt、描边 1.5；选中态用主色。
- 阴影：卡片轻投影（不透明度 6~8%），仅用于层级区分。

### 3.4 核心组件约定

| 组件 | 规范 |
| --- | --- |
| 主按钮 | 主色实心、圆角 10、高 48；文字白色 17 号 |
| 次按钮 | 白底 + 主色描边 |
| 卡片 | 白底圆角 12、轻投影、整卡可点 |
| 标签 | 13 号、圆角 6、浅色底 |
| 弹窗 | 圆角 16，最多两个操作按钮 |
| Toast | 轻提示，2 秒自动消失 |
| 导航栏 | 白底、标题居中 17 号 |

---

## 四、信息架构与导航

- 底部 Tab 固定 3 个：**首页 / 社区 / 我的**（不加第四个）。
- 首页内部分区：**拼豆 / 敬请期待**（分段控件，默认「拼豆」）。
- 页面地图（一期）：

```
首页 ─┬─ 拼豆 ─┬─ 预约（附近门店地图+列表 → 日期时段/人数/桌位 → 确认）
      │        ├─ 到店（输码核销 → 上钟 → 计时中 → 下钟 → 体验总结）
      │        └─ 会员套餐（权益卡 → 套餐选择 → 开通/续费）
      └─ 敬请期待（占位卡片）
社区 ── 信息流 → 作品详情/评论区 → 发布
我的 ── 资料区 / 卡包 / 点赞与收藏 / 个人作品 / 观看历史 / 我的订单
```

---

## 五、首页设计

### 5.1 首页结构

- 顶部：App 品牌名 + 当前城市（点击进入预约地图，默认定位附近门店）。
- 板块一「拼豆」：三个入口卡片并排，每入口 = 图标 + 名称（预约 / 到店 / 会员套餐），整卡可点。
- 板块二「敬请期待」：浅色占位卡片 + 一句话，不给按钮、不引导，保持简洁。

### 5.2 预约（拼豆 · 入口 1）—— 交互参考 Google Maps

- 首屏直接是「附近可预约门店」地图 + 底部门店卡片列表（半屏），**无需任何前置选择**。
- 门店卡片：门店名 / 距离 / 评分 / 最近可约时段，右侧「预约」按钮。
- 点门店进入预约页，流程只有三步：
  1. **日期 + 时间段**：横向日期条 + 时段网格（可约=主色、约满=置灰）；
  2. **到店人数**：步进器（− / 人数 / +，默认 1，上限按门店设定）；
  3. **桌位**：简单示意（列表或分区图）展示可选桌位，点选即选中。
- 确认页：一屏摘要（门店 / 日期时段 / 人数 / 桌位）+ 唯一主按钮「确认预约」。
- 原则：除「日期时段、人数、桌位」外，不再出现任何必填项。

### 5.3 到店（拼豆 · 入口 2）

- **输码核销页**：大号验证码输入（4~6 位，数字键盘自动弹出），下方主按钮「使用体验」。
- **核销成功**：全屏成功态（✓ 主色）+ 自动进入上钟；上钟后进入计时页。
- **计时页**（核心页面，信息极简）：居中大字「上钟时间」→ 实时「使用时长」；主按钮「下钟（结束体验）」。
- **下钟后**：体验总结页展示 上钟时间 / 下钟时间 / 使用时长，主按钮「完成」。
- 原则：上/下钟时间与时长是页面唯一焦点，不堆叠其他信息。

### 5.4 会员套餐（拼豆 · 入口 3）

- 顶部**会员权益卡**：会员编号、有效期、当前状态（未开通 / 生效中 / 已到期，用标签色区分）。
- **套餐区**：月 / 季 / 年 3 档卡片并排，展示期限 + 价格，选中卡片主色描边高亮。
- **权益区**：会员专属预约/到店价格、会员优惠券、专属活动，图标行 + 一行文字列示，不展开长文案。
- 底部：固定主按钮「开通会员 / 立即续费」；已开通则显示「续费」与到期倒计时。
- 状态明确：到期前 7 天显示警示标签「即将到期」。

---

## 六、社区页设计（参考微博社区）

- 顶部：标题「社区」+ 右侧主色发布按钮（+）。
- 内容流：默认「推荐」（「关注」Tab 二期可选）；卡片 = 作者行（头像 / 昵称 / 时间）+ 作品大图 + 文案（最多 3 行）+ 互动栏。
- 互动栏：**点赞 / 收藏 / 评论**，图标 + 数字，点按即反馈（图标变色）。
- 作品详情：大图全宽 + 文案 + 评论区（头像 / 昵称 / 内容 / 时间）。
- 发布：相册/拍照 → 选图（≤9 张）→ 一句话文案 + 标签 → 发布；成功回到信息流顶部。
- 空态：无内容时显示「还没有作品，来发布第一个吧」+ 主按钮。

---

## 七、我的页面设计

- 顶部资料区：圆形头像（64）+ 名称 + 会员标识（已开通则显示主色「会员」角标，点击进入会员页）。
- 功能列表（一列宫格：图标 + 名称 + 右箭头）：
  1. **卡包** —— 优惠券 / 体验券
  2. **点赞与收藏** —— 我赞过的 / 我收藏的（内部分 Tab）
  3. **个人作品** —— 我发布的作品（含草稿态）
  4. **观看历史** —— 浏览过的作品 / 门店记录
  5. **我的订单** —— 全部 / 待核销 / 进行中（上钟中）/ 已完成 / 已取消
- 每行右侧可显示角标（如待核销订单数）。
- 原则：入口即名称，不做多级引导；全列表统一样式。

---

## 八、交互与状态规范

| 状态 | 规范 |
| --- | --- |
| 加载 | 骨架屏优先，禁止整页菊花转 |
| 空态 | 图标/插画 + 一句引导 + 行动按钮 |
| 错误 | Toast 轻提示；关键操作失败给重试按钮 |
| 成功 | 全屏成功态或绿色反馈，明确下一步 |
| 禁用 | 置灰 + 原因提示（如「已约满」「已过期」） |
| 动效 | 200–300ms ease；转场用平台默认，不做花哨动画 |

---

## 九、一期页面清单（高保真交付）

| # | 页面 | 优先级 |
| --- | --- | --- |
| 1 | 登录 / 注册（手机号验证码） | P0 |
| 2 | 首页（拼豆 / 敬请期待） | P0 |
| 3 | 预约-附近门店（地图 + 列表） | P0 |
| 4 | 预约-选日期时段 / 人数 / 桌位 | P0 |
| 5 | 预约-确认页 | P0 |
| 6 | 到店-输码核销 | P0 |
| 7 | 到店-上钟 / 计时 / 下钟 | P0 |
| 8 | 到店-体验总结 | P0 |
| 9 | 会员套餐 | P0 |
| 10 | 社区-信息流 | P0 |
| 11 | 社区-作品详情 / 评论 | P0 |
| 12 | 社区-发布 | P0 |
| 13 | 我的-个人主页 | P0 |
| 14 | 我的-订单列表 | P0 |
| 15 | 我的-收藏 / 点赞 / 历史 / 卡包 | P1 |
| 16 | 空态 / 错误 / 加载等通用状态 | P0 |

---

## 十、设计验收口径

- 全部 P0 页面高保真定稿，标注尺寸、色值、字号与各状态；
- 核心流程（预约、到店、社区互动）可在原型上完整走通；
- 与《schedule.md》D1 检查点对齐：需求冻结签核完成前，设计稿定稿；
- 输出设计规范（色彩 / 字体 / 组件）供开发直接取用，减少还原偏差。
